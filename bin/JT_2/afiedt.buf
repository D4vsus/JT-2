DECLARE
	RESULT ALUMNOS.MATRICULA%TYPE;
	asd VARCHAR2(100);
BEGIN
	SELECT FLOOR(AVG(MATRICULA)) INTO RESULT FROM ALUMNOS WHERE CODIGO =  (SELECT CODIGO FROM CURSO WHERE DESCRIPCION =
	UPPER(
	'&DESC'
	));
	DB_INPUT.READ_LINE(ASD);
	DBMS_OUTPUT.PUT_LINE(RESULT);
END;
/
